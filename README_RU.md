# 🚀 Speedtest Monitor

[![Python 3.9+](https://img.shields.io/badge/python-3.9+-blue.svg)](https://www.python.org/downloads/)
[![UV](https://img.shields.io/badge/package%20manager-UV-blue)](https://github.com/astral-sh/uv)
[![License: MIT](https://img.shields.io/badge/License-MIT-yellow.svg)](https://opensource.org/licenses/MIT)

**Production-ready система мониторинга скорости интернета с уведомлениями в Telegram для неограниченного количества серверов. Работает на базе UV.**

[🇺🇸 English](README.md) | [🇷🇺 Русский](#русский)

---

## Русский

### 🎯 Возможности

- **🔄 Автоматический мониторинг** - Запланированные тесты скорости с интеллектуальной логикой повтора
- **📱 Интеграция с Telegram** - Уведомления в реальном времени с форматированными двуязычными отчетами
- **🖥️ Поддержка нескольких серверов** - Мониторинг множества серверов с централизованной отчетностью (Архитектура Master + Nodes)
- **🎯 Умные пороги** - Настраиваемые пороги скорости с визуальными индикаторами статуса
- **🔍 Автоопределение** - Автоматическая идентификация сервера и обнаружение команд speedtest
- **📊 Подробная отчетность** - Полная статистика: скорость, пинг, ISP, местоположение сервера, информация об ОС
- **⚡ На базе UV** - Молниеносное управление зависимостями с менеджером пакетов UV
- **⚙️ Гибкая конфигурация** - Конфигурация в YAML с поддержкой переменных окружения
- **🔧 Простое развертывание** - Автоматическая установка с интеграцией systemd/cron
- **📝 Production логирование** - Ротация логов с настраиваемым уровнем детализации
- **🛡️ Надежная обработка ошибок** - Логика повтора, graceful degradation, парсинг JSON/текста
- **🌐 Локализация** - Двуязычные отчеты (EN/RU) с настройками для каждого чата
- **📱 Интерактивный UI** - Инлайн-кнопки для переключения языка и режима просмотра (Компактный/Подробный)

### 📦 Быстрый старт

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/SokolovMO/speedtest_monitor.git
cd speedtest_monitor
chmod +x install.sh

# 2. Установите в нужном режиме
# Для одиночного сервера (Standalone):
./install.sh install single

# Для Master-сервера (Центральный агрегатор):
./install.sh install master

# Для Node (Агент мониторинга):
./install.sh install node
```

### 🏗️ Режимы архитектуры

Приложение поддерживает три режима работы:

1. **Single Mode** (По умолчанию): Запускает тест скорости и отправляет уведомление напрямую в Telegram. Лучше всего подходит для простых установок.
2. **Master Mode**: Работает как центральный сервер. Принимает отчеты от узлов (nodes) через HTTP API, агрегирует их и периодически отправляет общий отчет в Telegram.
3. **Node Mode**: Запускает тест скорости и отправляет результат на Master-сервер через HTTP API.

#### Локальная нода на Master (Опционально)

Сам Master-сервер по умолчанию не запускает тесты скорости. Если вы хотите мониторить скорость соединения самого Master-сервера, вы можете установить **Локальную ноду** на той же машине.

- Это создаст отдельный файл конфигурации (`config-master-node.yaml`) и systemd сервис (`speedtest-master-node`).
- Она отправляет отчеты на Master через `localhost`.
- В общем отчете она отображается как обычная нода (например, "Master Server").

### 🔐 Безопасность

Для связи Master и Node необходимо сгенерировать безопасный `api_token`. Он должен быть одинаковым на Master и всех Node.
Установщик поможет сгенерировать его, или используйте команду:

```bash
openssl rand -hex 32
```

### 🛠️ Требования

- **Python** 3.9 или выше
- **UV** менеджер пакетов (автоматически устанавливается через install.sh)
- **speedtest-cli** или официальный Ookla Speedtest (автоопределение)
- **Linux** (Ubuntu/Debian/RHEL/CentOS), **macOS** или **FreeBSD**
- **Telegram Bot** токен от [@BotFather](https://t.me/BotFather)

---

## 📖 Документация

### 📥 Руководство по установке

#### 1. Установка Master-сервера

Master-сервер собирает данные со всех узлов и отправляет уведомления в Telegram.

1. Запустите `./install.sh install master`
2. Следуйте интерактивным подсказкам для настройки:
   - Токен Telegram бота и Chat ID
   - **API Token** (автоматическая генерация или вручную)
   - Интервал отчетов и немедленная отправка
   - **Опционально:** Установка локальной ноды для мониторинга скорости самого мастера
3. Установщик автоматически обновит `config.yaml` и запустит сервис `speedtest-master`.

#### 2. Установка Node (Узла)

Узлы запускают тесты скорости и отправляют результаты на Master.

1. Запустите `./install.sh install node`
2. Следуйте интерактивным подсказкам для настройки:
   - **Node ID** (должен совпадать с записью в `nodes_meta` на Master)
   - **Master URL** (например, `http://MASTER_IP:8080/api/v1/report`)
   - **API Token** (должен совпадать с токеном Master)
3. Установщик автоматически обновит `config.yaml` и запустит таймер `speedtest-monitor`.

### 🔄 Руководство по миграции (Single -> Master/Node)

Если у вас уже есть Single сервер и вы хотите превратить его в Master:

1. **На сервере Master:**
   - Запустите `./install.sh install master`
   - Следуйте подсказкам для переключения в режим Master.
   - Опционально включите функцию "Локальная нода", если хотите продолжать запускать тесты на этом сервере.
   - Установщик сам переключит сервисы (отключит таймер монитора, включит сервис мастера).

2. **На серверах Node:**
   - Запустите `./install.sh install node`
   - Следуйте подсказкам для подключения к новому Master.

### 🤖 Автоматический режим (Auto Mode)

Для автоматизированного развертывания (Ansible, cloud-init и т.д.) используйте флаг `--auto`, чтобы пропустить все вопросы:

```bash
./install.sh install master --auto
# или
./install.sh install node --auto
```

**Примечание:** В автоматическом режиме установщик **не** настраивает `config.yaml` или `.env`. Вы должны создать эти файлы вручную или использовать инструменты управления конфигурацией для их перезаписи после установки.

### 📊 Архитектура кластера

```text
[ Node 1 (US) ] ──┐
                  │
[ Node 2 (DE) ] ──┼──> [ Master Server ] ──> [ Telegram Bot ]
                  │      (Агрегатор)
[ Node 3 (FI) ] ──┘           ^
                              │
[ Local Node ] ───────────────┘
(Опционально, на Master)
```

### ✅ Проверка установки

Чтобы проверить установку Master:

1. Проверьте статус сервиса:

   ```bash
   sudo systemctl status speedtest-master
   ```

2. Проверьте логи:

   ```bash
   sudo journalctl -u speedtest-master -n 50 --no-pager
   ```

3. Проверьте Health Endpoint:

   ```bash
   curl http://localhost:8080/health
   # Должен вернуть: {"status": "ok", "mode": "master", ...}
   ```

#### 4. Удаление

Чтобы удалить приложение и все сервисы:

```bash
./install.sh uninstall
```

Это остановит сервисы, удалит systemd юниты и опционально удалит директорию проекта.

### 🚀 Быстрый старт (читать по порядку)

1. **[📥 Установка](docs/installation_ru.md)** - Автоматическая установка с UV
2. **[⚙️ Конфигурация](docs/configuration_ru.md)** - Настройка .env и config.yaml
3. **[📅 Расписание](docs/scheduling-guide_ru.md)** - Настройка systemd/cron
4. **[🚀 Развертывание](docs/deployment_ru.md)** - Production развертывание

---

## 🏗️ Структура проекта

```text
speedtest-monitor/
├── 📄 .python-version          # Python 3.9
├── 📦 pyproject.toml           # UV конфигурация
├── 🔒 uv.lock                  # Зависимости (автоматически)
├── 📖 README.md / README_RU.md # Документация
├── ⚙️ config.yaml.example      # Шаблон конфигурации
├── 🔑 .env.example             # Шаблон секретов
├── 🚀 install.sh               # Автоматический установщик
│
├── 📁 speedtest_monitor/       # Основной код
│   ├── main.py                 # Точка входа
│   ├── config.py               # Загрузка конфигурации
│   ├── aggregator.py           # Master: Логика агрегации
│   ├── api.py                  # Master: HTTP API
│   ├── node_client.py          # Node: HTTP Клиент
│   ├── chat_prefs.py           # Master: Настройки чатов (SQLite)
│   ├── localization.py         # Переводы
│   ├── view_renderer.py        # Форматирование сообщений
│   ├── models.py               # Модели данных
│   ├── ...
│
├── 📁 systemd/                 # Автозапуск Linux
│   ├── speedtest-monitor.service
│   ├── speedtest-monitor.timer
│   └── speedtest-master.service # Master сервис
│
└── 📁 tests/                   # Тесты
```

### ⚡ Почему UV?

Этот проект использует [UV](https://github.com/astral-sh/uv) - современный, сверхбыстрый менеджер пакетов Python:

- **В 10-100 раз быстрее** чем pip
- **Воспроизводимые сборки** с поддержкой lockfile
- **Нулевая конфигурация** не требуется
- **Drop-in замена** для pip/venv
- **Кроссплатформенная** поддержка

### 📊 Пример уведомления в Telegram

```text
📊 Отчет о скорости интернета / Internet Speed Report

🖥 Сервер / Server: web-server-01 (Нью-Йорк, США)
📝 Описание / Description: Production веб-сервер
🆔 ID: web-01
🕐 Время / Time: 2025-10-26 15:30:45

📶 Результаты / Results:
⬇️ Загрузка / Download: 250.5 Mbps
⬆️ Отдача / Upload: 125.2 Mbps
📡 Пинг / Ping: 15.3 ms

📈 Статус / Status: 🚀⚡ Отлично / Excellent

🌐 Тестовый сервер / Test Server: Speedtest.net (NYC)
🏢 Провайдер / ISP: DigitalOcean
💻 ОС / OS: Ubuntu 22.04.3 LTS
```

### 🚀 Примеры использования

```bash
# Запустить speedtest один раз
uv run python -m speedtest_monitor.main

# Запустить с пользовательской конфигурацией
uv run python -m speedtest_monitor.main --config /path/to/config.yaml

# Включить debug логирование
uv run python -m speedtest_monitor.main --log-level DEBUG

# Проверить статус с systemd
sudo systemctl status speedtest-monitor.timer
sudo journalctl -u speedtest-monitor -f

# Запустить тесты
uv run pytest

# Форматировать код
uv run black speedtest_monitor/
uv run ruff check speedtest_monitor/
```

### 🤝 Участие в разработке

Приветствуются любые вклады! Пожалуйста, прочитайте [CONTRIBUTING.md](CONTRIBUTING.md) для получения рекомендаций.

### 📄 Лицензия

Этот проект распространяется под лицензией MIT - см. файл [LICENSE](LICENSE) для деталей.

### 👤 Автор

SokolovMO

- GitHub: [@SokolovMO](https://github.com/SokolovMO)
- Репозиторий: [speedtest_monitor](https://github.com/SokolovMO/speedtest_monitor)

### 🙏 Благодарности

- [UV](https://github.com/astral-sh/uv) - Современный менеджер пакетов Python
- [speedtest-cli](https://github.com/sivel/speedtest-cli) - Command-line speedtest  
- [Ookla Speedtest](https://www.speedtest.net/apps/cli) - Официальный speedtest CLI
- [aiogram](https://github.com/aiogram/aiogram) - Фреймворк для Telegram ботов
- [loguru](https://github.com/Delgan/loguru) - Библиотека логирования Python

---

**⭐ Если вы находите этот проект полезным, пожалуйста, поставьте звезду!**




