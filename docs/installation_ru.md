# 📥 Руководство по установке - Speedtest Monitor

[🇺🇸 English version](installation.md)

---

## Содержание

- [Требования](#требования)
- [Быстрая установка](#быстрая-установка)
- [Ручная установка](#ручная-установка)
- [Проверка](#проверка)
- [Устранение проблем](#устранение-проблем)

---

## Требования

### Системные требования

- **Операционная система**: Linux (Ubuntu 20.04+, Debian 11+, RHEL/CentOS 8+), macOS 11+, или FreeBSD
- **Python**: 3.9 или выше
- **Память**: Минимум 512MB RAM
- **Дисковое пространство**: 200MB свободного места

### Необходимое ПО

Установочный скрипт автоматически установит всё необходимое, но вы можете установить вручную:

1. **UV Менеджер пакетов** (устанавливается автоматически)
   ```bash
   curl -LsSf https://astral.sh/uv/install.sh | sh
   ```

2. **Speedtest CLI** (один из вариантов):
   - speedtest-cli: `pip install speedtest-cli`
   - Официальный Ookla: https://www.speedtest.net/apps/cli

3. **Telegram Бот**:
   - Создайте бота через [@BotFather](https://t.me/BotFather)
   - Получите токен бота и chat ID

---

## Быстрая установка

### Автоматическая установка (Рекомендуется)

```bash
# 1. Клонируйте репозиторий
git clone https://github.com/SokolovMO/speedtest_monitor.git
cd speedtest_monitor

# 2. Сделайте установщик исполняемым
chmod +x install.sh

# 3. Запустите установщик
./install.sh

# Установщик автоматически:
# - Проверит системные требования
# - Установит UV менеджер пакетов
# - Создаст виртуальное окружение
# - Установит все зависимости с помощью uv sync
# - Настроит systemd timer (опционально)
# - Настроит cron задание (альтернатива)
```

### Опции установки

```bash
# Автоматический режим (без запросов)
./install.sh --auto

# Пропустить systemd, использовать только cron
./install.sh --no-systemd

# Установить для конкретного пользователя
./install.sh --user myuser

# Показать справку
./install.sh --help
```

---

## Ручная установка

### Шаг 1: Установите UV

```bash
# Установите UV менеджер пакетов
curl -LsSf https://astral.sh/uv/install.sh | sh

# Добавьте UV в PATH (если не было сделано автоматически)
export PATH="$HOME/.cargo/bin:$PATH"

# Проверьте установку
uv --version
```

### Шаг 2: Клонируйте репозиторий

```bash
git clone https://github.com/SokolovMO/speedtest_monitor.git
cd speedtest_monitor
```

### Шаг 3: Создайте виртуальное окружение

```bash
# Создайте venv с помощью UV
uv venv

# Активируйте venv
source .venv/bin/activate  # Linux/macOS
```

### Шаг 4: Установите зависимости

```bash
# Установите с помощью UV sync (рекомендуется - использует lockfile)
uv sync

# Или установите в режиме разработки
uv pip install -e ".[dev]"
```

### Шаг 5: Настройте приложение

```bash
# Скопируйте шаблоны конфигурации
cp .env.example .env
cp config.yaml.example config.yaml

# Отредактируйте файлы конфигурации
nano .env           # Добавьте учетные данные Telegram
nano config.yaml    # Настройте параметры
```

### Шаг 6: Установите Speedtest CLI

Выберите один вариант:

**Вариант A: speedtest-cli (Python)**
```bash
uv pip install speedtest-cli
```

**Вариант B: Официальный Ookla Speedtest**
```bash
# Ubuntu/Debian
curl -s https://packagecloud.io/install/repositories/ookla/speedtest-cli/script.deb.sh | sudo bash
sudo apt-get install speedtest

# RHEL/CentOS
curl -s https://packagecloud.io/install/repositories/ookla/speedtest-cli/script.rpm.sh | sudo bash
sudo yum install speedtest

# macOS
brew install speedtest-cli
```

---

## Проверка

### Тестирование установки

```bash
# 1. Запустите один speedtest
uv run python -m speedtest_monitor.main

# 2. Проверьте логи
tail -f speedtest.log

# 3. Убедитесь, что уведомление в Telegram было отправлено
```

### Проверка настройки UV

```bash
# Проверьте версию UV
uv --version

# Список установленных пакетов
uv pip list

# Проверьте версию Python
python --version

# Проверьте зависимости
uv pip check
```

### Проверка команд Speedtest

```bash
# Тест speedtest-cli
speedtest-cli --version

# Тест официального speedtest
speedtest --version

# Запустите ручной speedtest
speedtest --format=json
```

---

## Устранение проблем

### UV не найден

```bash
# Переустановите UV
curl -LsSf https://astral.sh/uv/install.sh | sh

# Добавьте в PATH постоянно
echo 'export PATH="$HOME/.cargo/bin:$PATH"' >> ~/.bashrc
source ~/.bashrc
```

### Ошибки прав доступа

```bash
# Исправьте владельца
sudo chown -R $USER:$USER /opt/speedtest-monitor

# Исправьте права
chmod +x install.sh
chmod 755 speedtest_monitor/
```

### Отсутствующие зависимости

```bash
# Переустановите все зависимости
uv sync --reinstall

# Или вручную
uv pip install -e ".[dev]"
```

### Команда Speedtest не найдена

```bash
# Проверьте доступные команды
which speedtest speedtest-cli

# Установите speedtest-cli
uv pip install speedtest-cli

# Или скачайте официальный бинарник
wget https://install.speedtest.net/app/cli/ookla-speedtest-1.2.0-linux-x86_64.tgz
tar xvf ookla-speedtest-*.tgz
sudo mv speedtest /usr/local/bin/
```

### Проблемы с версией Python

```bash
# Проверьте версию Python
python --version

# Установите Python 3.9+ если нужно (Ubuntu/Debian)
sudo apt update
sudo apt install python3.9 python3.9-venv python3-pip

# Обновите .python-version если нужно
echo "3.9" > .python-version
```

---

## Следующие шаги

После успешной установки:

1. **Настройка**: Прочитайте [Руководство по конфигурации](configuration_ru.md)
2. **Расписание**: Прочитайте [Руководство по расписанию](scheduling-guide_ru.md)
3. **Развертывание**: Посмотрите [Руководство по развертыванию](deployment_ru.md)
4. **Устранение проблем**: См. [Устранение проблем](troubleshooting.md) для частых проблем

---

## Поддержка

- **Issues**: https://github.com/SokolovMO/speedtest_monitor/issues
- **Discussions**: https://github.com/SokolovMO/speedtest_monitor/discussions
